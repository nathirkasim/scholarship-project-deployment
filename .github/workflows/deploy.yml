name: Build & Deploy

on:
  push:
    branches: [main]

env:
  AWS_REGION: us-east-1
  ECR_REGISTRY: 954930678370.dkr.ecr.us-east-1.amazonaws.com
  API_REPO: scholarship-api
  WEB_REPO: scholarship-web
  ML_REPO: scholarship-ml
  ASG_NAME: nathir-webapp-asg

jobs:
  build-and-deploy:
    name: Build, Push & Deploy
    runs-on: ubuntu-latest

    steps:
      # ── 1. Checkout ─────────────────────────────────────────────────────
      - name: Checkout code
        uses: actions/checkout@v4

      # ── 2. AWS Credentials ──────────────────────────────────────────────
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      # ── 3. ECR Login ─────────────────────────────────────────────────────
      - name: Login to Amazon ECR
        uses: aws-actions/amazon-ecr-login@v2

      # ── 4. Set image tag (short SHA) ─────────────────────────────────────
      - name: Set image tag
        id: tag
        run: echo "TAG=${GITHUB_SHA::8}" >> $GITHUB_OUTPUT

      # ── 5. Build & push API image ────────────────────────────────────────
      - name: Build & push API
        uses: docker/build-push-action@v5
        with:
          context: .
          file: apps/api/Dockerfile
          push: true
          tags: |
            ${{ env.ECR_REGISTRY }}/${{ env.API_REPO }}:${{ steps.tag.outputs.TAG }}
            ${{ env.ECR_REGISTRY }}/${{ env.API_REPO }}:latest

      # ── 6. Build & push Web image ────────────────────────────────────────
      - name: Build & push Web
        uses: docker/build-push-action@v5
        with:
          context: apps/web
          file: apps/web/Dockerfile
          push: true
          tags: |
            ${{ env.ECR_REGISTRY }}/${{ env.WEB_REPO }}:${{ steps.tag.outputs.TAG }}
            ${{ env.ECR_REGISTRY }}/${{ env.WEB_REPO }}:latest

      # ── 7. Build & push ML image ─────────────────────────────────────────
      - name: Build & push ML service
        uses: docker/build-push-action@v5
        with:
          context: ml-service
          file: ml-service/Dockerfile
          push: true
          tags: |
            ${{ env.ECR_REGISTRY }}/${{ env.ML_REPO }}:${{ steps.tag.outputs.TAG }}
            ${{ env.ECR_REGISTRY }}/${{ env.ML_REPO }}:latest

      # ── 8. Resolve instance ID from ASG ──────────────────────────────────
      - name: Get EC2 instance ID from ASG
        id: get-instance
        run: |
          INSTANCE_ID=$(aws autoscaling describe-auto-scaling-groups \
            --auto-scaling-group-names "${{ env.ASG_NAME }}" \
            --query "AutoScalingGroups[0].Instances[?HealthStatus=='Healthy'] | [0].InstanceId" \
            --output text)

          if [ -z "$INSTANCE_ID" ] || [ "$INSTANCE_ID" = "None" ]; then
            echo "ERROR: No healthy instance found in ASG ${{ env.ASG_NAME }}"
            exit 1
          fi

          echo "Found instance: $INSTANCE_ID"
          echo "INSTANCE_ID=$INSTANCE_ID" >> $GITHUB_OUTPUT

      # ── 9. Deploy via SSM (runs as root — docker socket accessible, .env sourced explicitly) ──
      - name: Deploy via SSM
        run: |
          INSTANCE_ID="${{ steps.get-instance.outputs.INSTANCE_ID }}"

          COMMAND_ID=$(aws ssm send-command \
            --instance-ids "$INSTANCE_ID" \
            --document-name "AWS-RunShellScript" \
            --comment "Deploy ${GITHUB_SHA::8}" \
            --parameters 'commands=[
              "#!/bin/bash",
              "set -e",
              "cd /home/ubuntu/app",
              "git config --global --add safe.directory /home/ubuntu/app",
              "git fetch origin main",
              "git reset --hard origin/main",
              "echo === Logging into ECR ===",
              "aws ecr get-login-password --region us-east-1 | docker login --username AWS --password-stdin 954930678370.dkr.ecr.us-east-1.amazonaws.com",
              "echo === Pulling new images ===",
              "set -a && source /home/ubuntu/app/.env && set +a && docker compose -f /home/ubuntu/app/docker-compose.yml -f /home/ubuntu/app/docker-compose.override.yml pull web api ml-service",
              "echo === Restarting app containers ===",
              "set -a && source /home/ubuntu/app/.env && set +a && docker compose -f /home/ubuntu/app/docker-compose.yml -f /home/ubuntu/app/docker-compose.override.yml up -d --no-deps web api ml-service",
              "sleep 20",
              "set -a && source /home/ubuntu/app/.env && set +a && docker compose -f /home/ubuntu/app/docker-compose.yml -f /home/ubuntu/app/docker-compose.override.yml ps",
              "echo === Running migrations ===",
              "set -a && source /home/ubuntu/app/.env && set +a && docker compose -f /home/ubuntu/app/docker-compose.yml -f /home/ubuntu/app/docker-compose.override.yml exec -T api npx prisma migrate deploy 2>&1 || true",
              "docker image prune -f",
              "echo === Deploy complete ==="
            ]' \
            --timeout-seconds 600 \
            --query "Command.CommandId" \
            --output text)

          echo "SSM Command ID: $COMMAND_ID"
          echo "COMMAND_ID=$COMMAND_ID" >> $GITHUB_ENV

      # ── 10. Poll until done (40 × 15s = 10 min max) ──────────────────────
      - name: Wait for deploy to complete
        run: |
          INSTANCE_ID="${{ steps.get-instance.outputs.INSTANCE_ID }}"
          echo "Polling SSM command $COMMAND_ID..."

          for i in $(seq 1 40); do
            STATUS=$(aws ssm get-command-invocation \
              --command-id "$COMMAND_ID" \
              --instance-id "$INSTANCE_ID" \
              --query "Status" --output text 2>/dev/null || echo "Pending")

            echo "Attempt $i/40 — Status: $STATUS"

            if [ "$STATUS" = "Success" ]; then
              echo "=== Deploy Output ==="
              aws ssm get-command-invocation \
                --command-id "$COMMAND_ID" \
                --instance-id "$INSTANCE_ID" \
                --query "StandardOutputContent" --output text
              echo "Deploy succeeded."
              exit 0
            elif [ "$STATUS" = "Failed" ] || [ "$STATUS" = "Cancelled" ] || [ "$STATUS" = "TimedOut" ]; then
              echo "=== Deploy Output ==="
              aws ssm get-command-invocation \
                --command-id "$COMMAND_ID" \
                --instance-id "$INSTANCE_ID" \
                --query "{Out: StandardOutputContent, Err: StandardErrorContent}" --output json
              echo "ERROR: Deploy failed with status: $STATUS"
              exit 1
            fi

            sleep 15
          done

          echo "ERROR: Timed out waiting for deploy (10 min)"
          exit 1
